# Непрерывная проверка соответствия: AWS Config и Security Hub

Вместо разовой проверки перед аудитом инфраструктура проверяется постоянно:
AWS Config записывает конфигурацию ресурсов, Security Hub сверяет её с наборами
контролей и выставляет оценку. Новая находка уровня CRITICAL или HIGH приходит
письмом.

## Что включено

| Что | Как | Где в коде |
|---|---|---|
| AWS Config | запись 25 типов ресурсов, из которых состоит эта инфраструктура | [monitoring.tf](../terraform/monitoring.tf) |
| Правила Config | 8 управляемых правил: шифрование, публичный доступ, порты управления | [monitoring.tf](../terraform/monitoring.tf) |
| Security Hub | наборы AWS Foundational Security Best Practices v1.0.0 и CIS AWS Foundations Benchmark v3.0.0 | [security-hub.tf](../terraform/security-hub.tf) |
| Оповещения | Security Hub → EventBridge → SNS, только CRITICAL и HIGH в состоянии NEW | [security-hub.tf](../terraform/security-hub.tf) |

Оба сервиса выключены по умолчанию и включаются переменными `enable_config`
и `enable_security_hub`: они тарифицируются за каждый записанный элемент
конфигурации и за каждую проверку.

## Оценка до и после

| Замер | Дата | Оценка | Провалено контролей |
|---|---|---|---|
| До исправлений | | | |
| После исправлений | | | |

Скриншоты: `screenshots/before.png`, `screenshots/after.png`.

## Разбор находок

Каждая находка попадает в одну из трёх категорий: исправлена кодом, подавлена
с обоснованием, принята как задокументированный риск.

### Исправлено

| Контроль | Уровень | Что было не так | Чем исправлено | Коммит |
|---|---|---|---|---|
| | | | | |

### Подавлено

| Контроль | Уровень | Почему проверка неприменима |
|---|---|---|
| | | |

### Принято как риск

| Контроль | Уровень | Почему остаётся | Компенсация |
|---|---|---|---|
| | | | |

## Чего эти проверки не покажут

## Стоимость

| Сервис | За что берут | Замер за время проверки |
|---|---|---|
| AWS Config | за записанный элемент конфигурации и за оценку правила | |
| Security Hub | за каждую проверку контроля | |

После сдачи оба сервиса выключаются: `enable_config = false`,
`enable_security_hub = false`, затем `terraform apply`.
