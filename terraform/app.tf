################################################
#               Слой приложения                #
################################################
# Вычислительный слой в приватных подсетях, без публичных адресов:
# снаружи до него можно добраться только через балансировщик.
#
# IMDSv2 обязателен. Без токена метаданные не отдаются, и уязвимость
# вида SSRF в приложении не даёт прочитать учётные данные роли
# по адресу 169.254.169.254.
#
# Диски зашифрованы: незашифрованный том — первая находка любого
# сканера соответствия.
#
# Реквизиты базы приложение читает из Secrets Manager своей ролью,
# а не получает через user data: содержимое user data видно
# в метаданных инстанса.
#
# Доступ инженера — только Session Manager: ни ключей SSH,
# ни bastion-хоста.
