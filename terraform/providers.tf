################################################
#              Версии и провайдер              #
################################################
# Версии провайдеров закреплены: без этого конфигурация однажды меняет
# поведение сама, без единого коммита в репозитории.
#
# Состояние хранится в S3 с шифрованием и блокировкой. Это не только
# удобство командной работы: в состоянии лежат сгенерированные пароли,
# поэтому доступ к бакету равен доступу к секретам проекта.
#
# default_tags вешает теги на все ресурсы сразу. По ним ищут владельца
# ресурса и разбирают счёт, а «ресурсы без владельца» — типовая находка
# в отчёте о соответствии.
